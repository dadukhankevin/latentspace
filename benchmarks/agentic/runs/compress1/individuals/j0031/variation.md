the base methodology, BUT replace the parent's realized-surprise novelty calibration with a bounded age-aware escape prior: derive each context's escape mass from its distinct-continuation count plus a coarse measure of how long it has been since that context was last observed, cap that mass per context, and mirror the same deterministic timestamp update during decoding.

contradicts_base: false

the base methodology, BUT use one exclusion-PPM arithmetic engine to compare two deterministic whole-message representations: the raw byte stream for repeated local phrases, and a global BWT→MTF stream whose zero ranks become bijective-binary RUNA/RUNB digits terminated by literals or EOF; emit the shorter complete stream, marking its mode in the length header. In either mode, descend through sparse order-4 contexts only after coding an escape for an absent symbol while excluding continuations already offered above, then finish with an adaptive order-0 floor. Bounded local rescaling keeps the shared model responsive, so Parent A’s rank clustering is selected when it helps and Parent B’s longest-context probabilities are retained when local phrases win, without charging lower-order rows twice.

contradicts_base: false

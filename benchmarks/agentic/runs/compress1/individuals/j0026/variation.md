the base methodology, BUT replace the parent’s implemented LZ/PPM-versus-BWT selector with a fresh suffix-array BWT and move-to-front compressor: arithmetic-code zero-run ranks with an explicit terminator, send a compact presence bitmap so sparse text uses a smaller alphabet, retain a full-alphabet BWT branch for dense data, and choose the shortest of those branches and raw bytes. The terminator removes a fixed rank-count field while preserving self-delimiting decoding; the bitmap and branch selection capture the parent’s useful input-adaptive behavior without copying its token parser.

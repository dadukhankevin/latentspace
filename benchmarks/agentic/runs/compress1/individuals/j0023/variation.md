the base methodology, BUT the parent code actually chooses the shortest of three blobs: a backward-DP 3-byte-indexed LZ parse with recent matches and order-2/1/0 PPM arithmetic coding, a full sentinel BWT with per-byte MTF, binary rank-zero run encoding, and adaptive rank coding, or raw bytes. I replace that BWT path with explicit maximal BWT runs, move each run symbol to front once, and interleave separate adaptive rank and run-length models in one range stream, starting MTF from a fixed text-frequency alphabet; raw bytes remain the fallback.

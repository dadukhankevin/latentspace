The base methodology, BUT retain the parent’s field-aware dynamic-programming path search and change only the literal representation: use an adaptive order-3 PPM range model with context exclusion and escape frequency 3. The path planner uses a scaled global-frequency estimate for literal transitions so its costs approximate that emitted model; match distances and lengths remain gamma-coded and priced in the same future-cost recurrence.

contradicts_base: false

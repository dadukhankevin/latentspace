the base methodology, BUT replace the literal-only greedy choice with an exclusion-based PPM parse: at every byte, consult sparse contexts of orders four down to one, code an adaptive escape when the continuation is absent, remove symbols already offered by longer contexts, and finish in a globally adaptive order-0 distribution. Update every suffix context after each reconstructed byte and age rows locally, so long repeated phrases receive precise probabilities while unseen input remains losslessly covered.

contradicts_base: false

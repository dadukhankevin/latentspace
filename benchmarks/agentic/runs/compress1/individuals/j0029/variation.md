the base methodology, BUT replace serial escape/backoff with a direct interpolated 256-symbol mixture: retain adaptive global and order-1-to-4 evidence, then add bounded order-6, order-8, and order-10 continuation rows that contribute only when those exact suffixes have appeared before. Age every adaptive row and cap the influence of a single high-order observation, while keeping every byte representable. This exploits repeated prose and code phrases that order 4 misses without making one-off long contexts brittle.
